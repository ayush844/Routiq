{
  "name": "routiq-monorepo",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:cli": "pnpm --filter ./apps/cli build",
    "publish:cli": "pnpm --filter ./apps/cli publish --access public"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "tsx": "^4.22.3",
    "turbo": "^2.9.14",
    "typescript": "^6.0.3"
  }
}